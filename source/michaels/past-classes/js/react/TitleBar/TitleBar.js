import React from 'react';

/**
 * A titlebar with a flex layout.
 * @param {Object} props
 */
export default function TitleBar({ children }) {
    return <div className="c-TitleBar">{children}</div>;
}
