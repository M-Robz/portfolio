/* ==== index ====
 *
 * Re-exports the controller so that the `interactive-list` package can be
 * imported into `main.js` without specifically referencing the controller
 * component.
 */
module.exports = require('./controller');
